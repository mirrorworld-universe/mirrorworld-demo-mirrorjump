using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GateDecoradeSystem : UpdateSystem
{
    public override void OnSystemUpdate(List<Entity> entities)
    {
        //找到主角组件

        //监测他的高度

        //根据他的高度增加/移除台阶
        //Entity blockEntity = GameWorld.Instance.AddNewEntity();
        //BlockCom baseComponent = new BlockCom();
        //baseComponent.h = 1;
        //baseComponent.x = 2;
        //baseComponent.state = 'wait';
        //blockEntity.AddComponent(baseComponent);

        //根据他的高度增加/移除云彩
        //'die';
        
    }

    private void LogicController()
    {

    }
}
